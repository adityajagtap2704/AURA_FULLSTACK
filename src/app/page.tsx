export default function Home() {
  return (
    <main className="flex min-h-full flex-1 items-center justify-center p-8">
      <p className="text-sm text-neutral-500">
        AURA API — see the frontend app for the product UI.
      </p>
    </main>
  );
}
